{
  "buildCommand": "npx prisma generate && npx prisma db push --accept-data-loss && npx tsx prisma/seed.ts && npm run build:standalone",
  "outputDirectory": ".next",
  "installCommand": "npm install",
  "framework": "nextjs"
}
